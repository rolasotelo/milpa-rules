export enum SlotType {
  MILPA = "milpa",
  EDGE = "edge",
}

export enum ItemCode {
  CORN = "corn",
  BEANS = "beans",
}

export enum CardCode {
  CORN = "corn",
  BEANS = "beans",
}

export enum TurnStatus {
  STARTED = "started",
  WAITING = "waiting",
}
